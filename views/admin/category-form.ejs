<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= category ? 'Edit' : 'Create' %> Category - Multiverse Cloud Admin
    </title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap"
        rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    <link rel="stylesheet" href="/css/admin.css">
</head>

<body class="admin-body">
    <%- include('partials/sidebar', { active: 'categories' , admin: admin }) %>

        <div class="admin-main">
            <header class="admin-header">
                <button class="sidebar-toggle" id="sidebarToggle"><i class="fas fa-bars"></i></button>
                <h2>
                    <%= category ? 'Edit' : 'Create' %> Category
                </h2>
                <div class="admin-user">
                    <a href="/admin/categories" class="btn btn-outline btn-sm"><i class="fas fa-arrow-left"></i>
                        Back</a>
                </div>
            </header>

            <div class="admin-content">
                <% if (error) { %>
                    <div class="alert alert-error">
                        <i class="fas fa-exclamation-circle"></i>
                        <%= error %>
                    </div>
                    <% } %>

                        <div class="form-card">
                            <form action="/admin/categories/<%= category ? 'edit/' + category._id : 'create' %>"
                                method="POST" enctype="multipart/form-data">
                                <div class="form-group">
                                    <label for="name">Category Name *</label>
                                    <input type="text" id="name" name="name"
                                        value="<%= category ? category.name : '' %>"
                                        placeholder="e.g. Minecraft Hosting" required>
                                </div>
                                <div class="form-group">
                                    <label for="description">Description *</label>
                                    <textarea id="description" name="description" rows="4"
                                        placeholder="Describe this hosting category..."
                                        required><%= category ? category.description : '' %></textarea>
                                </div>
                                <div class="form-group">
                                    <label for="image">Category Image</label>
                                    <% if (category && category.image) { %>
                                        <div class="current-image">
                                            <img src="<%= category.image %>" alt="Current image">
                                            <p class="help-text">Upload a new image to replace the current one</p>
                                        </div>
                                        <% } %>
                                            <input type="file" id="image" name="image" accept="image/*">
                                            <p class="help-text">Recommended: 400x300px, Max 5MB (JPG, PNG, WebP)</p>
                                </div>
                                <div class="form-actions">
                                    <button type="submit" class="btn btn-primary btn-lg">
                                        <i class="fas fa-save"></i>
                                        <%= category ? 'Update' : 'Create' %> Category
                                    </button>
                                    <a href="/admin/categories" class="btn btn-outline btn-lg">Cancel</a>
                                </div>
                            </form>
                        </div>
            </div>
        </div>
        <script src="/js/admin.js"></script>
</body>

</html>